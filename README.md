# word2vec


```
resultround.dill = Round off result from the given dataset

resultstatusDF.dill and intconverted.dill are used for testing process could be ignored .


modelwithint is the model , which has 30,000 epochs trained with int dataset and rounded off value.

```
